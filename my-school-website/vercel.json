{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static-build" },
    { "src": "api/contact.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.ts" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
